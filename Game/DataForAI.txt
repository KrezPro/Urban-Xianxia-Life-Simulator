17. Metro bundler НЕ ПОДДЕРЖИВАЕТ динамический require(moduleId) с переменной — только статические импорты. Для graceful degradation при отсутствии нативного модуля использовать статические import + try/catch вокруг вызовов.