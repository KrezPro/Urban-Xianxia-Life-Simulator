# ВЫУЧЕННЫЕ УРОКИ (AiCoder)

## Урок 1: Hermes и таблицы GameConstants
Чтение отсутствующих таблиц GameConstants (например, EVENT_* таблиц) напрямую крашит Hermes
ошибкой "Cannot convert undefined value to object". Все таблицы читать ТОЛЬКО через
tableNumber(table, key, fallback) с fallback-значением.

## Урок 2: Навигация и журнал
navigation.navigate('Log') запрещён: журнал открывается ТОЛЬКО модально из LifeScreen
(LogScreen с onClose). Вкладка Log скрыта из tab bar.

## Урок 3: NotificationToast
Таймер автоскрытия запускается от момента монтирования тоста, а не от createdAt,
иначе уведомление исчезает мгновенно.

## Урок 4: Адаптивность вкладки Мир (ШАГ 27 -> ШАГ 28)
Ошибка: auto-ScrollView "fallback" + порог compact 780pt.
- Реальные телефоны имеют окно 800-844pt (360x800 dp даёт около 810pt), поэтому compact НЕ
  включался, контент не сжимался и не вмещался.
- Fallback включал ScrollView: пользователь видел прокрутку вместо сжатия (нарушение
  договорённости "всё закреплено и влазит на любом экране").
- Большой paddingBottom (84-96) + flex-остаток cardsArea давали мёртвую чёрную полосу снизу.
Решение: ScrollView во вкладке Мир НЕ использовать вообще. Контент сжимается и закрепляется.

## Урок 5: Приоритет договорённостей по UX
Если пользователь договорился "контент закреплён и влезает на любой экран", нельзя подменять
это скроллом. Лучше сжать типографику и отступы, чем включать прокрутку.
Пороги адаптивности выбирать по реальным размерам окон устройств (800-844pt), а не "на глаз".

## Урок 6: XML-безопасность ответов AiCoder
Вне CDATA-секций (commit_message, служебный текст) ЗАПРЕЩЕНО использовать символ "меньше"
перед числами или словами: парсер принимает его за начало XML-тега и падает с ошибкой
"invalid XML name". Внутри commit_message писать словами: "порог 900pt", "ниже 700pt".

## Урок 7: Центровка и автомасштабирование hero-блока (ШАГ 28 -> ШАГ 29)
Пользователь несколько раз повторял требования: эмодзи по центру и авто-масштаб под экран.
- Ошибка центровки: бейдж лежал в левой flex-зоне (flex: 1) и оказывался левее центра карточки.
  Решение: блок возраста закрепить position absolute справа (right 0, top 0, bottom 0),
  а бейдж центрировать по всей ширине heroTopRow через justifyContent center.
- Повторяющиеся требования пользователя выполнять с первого раза и закладывать сразу.

## Урок 8: Настоящий адаптивный дизайн (ШАГ 29 -> ШАГ 30)
Пользователь потребовал: экран автоматически заполняется и всё автоматически масштабируется
(адаптивный дизайн), ручные тиры compact/tiny не дали этого эффекта.
Решение: единый непрерывный коэффициент scale = clamp(windowHeight / 800, 0.75, 1.35) и
хелпер sw(v) = Math.round(v * scale); все paddings/margins/шрифты/бары/чипы через sw().
Автозаполнение: карточка статов получает flex: 1 и съедает всю свободную высоту экрана;
каждая StatRow обёрнута в View с flex: 1 и justifyContent center — строки распределяются
равномерно, мёртвых полос нет ни на одном экране. StatRow принимает проп scale
(масштабирует бейдж/иконку/шрифты/padding; дефолт 1 — другие экраны не меняются).
Ручные тиры compact/tiny удалены полностью.

## Урок 9: Перетаскиваемый FAB не участвует в компоновке (ШАГ 30 -> ШАГ 31)
Пользователь указал: кнопка "+1 год" двигается при зажатии, поэтому резервировать под неё
место (paddingBottom) НЕЛЬЗЯ — иначе экран не растягивается полностью по вертикали.
Решение: paddingBottom контента равен боковым отступам (sw(16)); DraggableGrowButton —
чистый абсолютный overlay поверх контента, не участвует в расчётах высоты.
Также: эмодзи аватара должны меняться для ВСЕХ стадий Дао и возрастов — введена матрица
"возрастная группа x тир стадии" (Смертный/Ци/Основание/Ядро/Бессмертный) с видами
движения crawl/breathe/levitate. Повторяющиеся требования выполнять с первого раза.